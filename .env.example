# This repo only. Do not run `npx vercel ai-gateway setup` — that rewrites
# Claude / Cursor / Grok configs globally.

# From vercel.com → AI Gateway → API keys (or `vercel ai-gateway api-keys create`)
# The app hardcodes typesafe-ai/jev; this key is only used for that call.
AI_GATEWAY_API_KEY=
